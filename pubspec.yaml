name: gr_camera_detector
description: >-
  Detect photos taken with Ricoh GR camera series via EXIF metadata
  and filename patterns. Supports all GR models from GR DIGITAL to GR IV.
version: 0.1.0
homepage: https://github.com/YOUR_USERNAME/gr_camera_detector
repository: https://github.com/YOUR_USERNAME/gr_camera_detector
issue_tracker: https://github.com/YOUR_USERNAME/gr_camera_detector/issues

environment:
  sdk: ^3.0.0

dependencies:
  exif: ^3.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

topics:
  - exif
  - ricoh
  - camera
  - image-processing
